"""Settings for the metadata clients package.

This package currently does not have package-specific settings —
all configuration flows through the aggregated ``Settings`` in
``evoscholar.literature_qa.settings``. This module is reserved for
future per-package settings (e.g., per-provider timeouts / API keys).
"""

from __future__ import annotations